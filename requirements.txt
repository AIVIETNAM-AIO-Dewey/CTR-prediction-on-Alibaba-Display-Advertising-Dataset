# ==========================================
# Core Data Processing & Utilities
# ==========================================
numpy>=1.24.0
pandas>=2.0.0
polars>=0.20.0          # Xử lý dữ liệu lớn (như raw_sample.csv ~1GB) với tốc độ cao & tiết kiệm RAM
pyarrow>=14.0.0         # Tối ưu đọc/ghi định dạng Parquet/Feather
scipy>=1.10.0
tqdm>=4.65.0            # Progress bar
joblib>=1.3.0           # Lưu/tải model & xử lý song song
pyyaml>=6.0             # Quản lý cấu hình dự án qua file config (.yaml)

# ==========================================
# Feature Engineering & Selection
# ==========================================
scikit-learn>=1.3.0     # Các phương pháp Feature Selection (RFE, Mutual Info, SelectKBest, v.v.)
category-encoders>=2.6.0 # Target Encoding, CatBoost Encoding cho high-cardinality features

# ==========================================
# Tree-based Machine Learning Models
# ==========================================
lightgbm>=4.0.0         # Model Gradient Boosting tối ưu cho CTR và bảng dữ liệu lớn
xgboost>=2.0.0          # Extreme Gradient Boosting
catboost>=1.2.0         # Tự động xử lý biến phân loại (Categorical Features) rất tốt

# ==========================================
# Model Tuning & Interpretability
# ==========================================
optuna>=3.3.0           # Tự động tìm kiếm siêu tham số (Hyperparameter Tuning)
shap>=0.42.0            # Giải thích mô hình và phân tích tầm quan trọng của Features (Feature Importance)

# ==========================================
# Visualization & Exploratory Data Analysis (EDA)
# ==========================================
matplotlib>=3.7.0
seaborn>=0.12.0

# ==========================================
# Development & Notebook Environment
# ==========================================
ipykernel>=6.25.0
jupyter>=1.0.0
